'use strict';

// Bump these whenever the corresponding public legal page changes substantively —
// kept in one place so order records always snapshot the version that was actually accepted.
module.exports = {
    TERMS_VERSION: '2.0',
    REFUND_POLICY_VERSION: '2.0'
};
